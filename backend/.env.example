# Copy to backend/.env for local development

APP_ENV=dev
HTTP_ADDR=:8080

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dms
DB_USER=dms
DB_PASSWORD=dms_dev_password
DB_SSLMODE=disable
DB_MAX_CONNS=4

# JWT
JWT_SECRET=CHANGE_ME_DEV_ONLY
